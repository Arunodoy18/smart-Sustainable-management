╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║   🚀 SMART WASTE MANAGEMENT - LOCAL SETUP                    ║
║   ✅ Ready to Run - No Docker, No Azure, Pure Local         ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────┐
│  STEP 1: OPEN FIRST TERMINAL (Backend)                        │
└───────────────────────────────────────────────────────────────┘

cd c:\dev\Smart-waste-ai
.\run-backend.ps1

✓ Wait for: "Application startup complete - ready to serve requests"

┌───────────────────────────────────────────────────────────────┐
│  STEP 2: OPEN SECOND TERMINAL (Frontend)                      │
└───────────────────────────────────────────────────────────────┘

cd c:\dev\Smart-waste-ai
.\run-frontend.ps1

✓ Wait for: "✓ Ready in [time]"

┌───────────────────────────────────────────────────────────────┐
│  STEP 3: OPEN BROWSER                                         │
└───────────────────────────────────────────────────────────────┘

http://localhost:3000

✓ Should see: Landing page with "Waste Management Reimagined"

┌───────────────────────────────────────────────────────────────┐
│  STEP 4: TEST AUTHENTICATION                                  │
└───────────────────────────────────────────────────────────────┘

1. Click "Sign Up" button (top right)
2. Fill form:
   Name: Test User
   Email: test@example.com
   Password: password123
   Role: User
3. Click "Create Account"
4. ✓ Should auto-login and redirect to Dashboard

┌───────────────────────────────────────────────────────────────┐
│  🎉 SUCCESS! APP IS RUNNING                                   │
└───────────────────────────────────────────────────────────────┘

📍 Service URLs:
   Frontend:  http://localhost:3000
   Backend:   http://localhost:8080
   API Docs:  http://localhost:8080/docs
   Health:    http://localhost:8080/health

🎨 Available Pages:
   /                  → Landing page
   /auth?mode=signup  → Signup
   /auth?mode=login   → Login
   /dashboard         → Dashboard (protected)
   /classify          → Waste classifier
   /analytics         → Analytics
   /history           → History
   /settings          → Settings
   /profile           → Profile

🔐 Features Working:
   ✅ User authentication
   ✅ JWT tokens
   ✅ Protected routes
   ✅ Clean UI with dark theme
   ✅ Responsive design
   ✅ Loading states
   ✅ Error handling
   ✅ Toast notifications

┌───────────────────────────────────────────────────────────────┐
│  🐛 TROUBLESHOOTING                                           │
└───────────────────────────────────────────────────────────────┘

Problem: Backend won't start
Fix: Check if Python venv exists
     cd backend
     python -m venv venv
     .\venv\Scripts\pip.exe install -r requirements.txt

Problem: "Failed to fetch" error
Fix: 1. Verify backend is running: http://localhost:8080/health
     2. Check frontend/.env.local has: NEXT_PUBLIC_API_URL=http://localhost:8080
     3. Restart frontend

Problem: Port 8080 in use
Fix: netstat -ano | findstr :8080
     taskkill /PID <pid> /F

Problem: Database error
Fix: Edit backend/.env, change to SQLite:
     DATABASE_URL=sqlite:///./waste_management.db

┌───────────────────────────────────────────────────────────────┐
│  📝 CONFIGURATION FILES                                       │
└───────────────────────────────────────────────────────────────┘

backend/.env:
  PORT=8080
  DATABASE_URL=postgresql://postgres:postgres@localhost/waste_management
  SECRET_KEY=dev-secret-key-change-in-production
  BACKEND_CORS_ORIGINS=["http://localhost:3000"]

frontend/.env.local:
  NEXT_PUBLIC_API_URL=http://localhost:8080

┌───────────────────────────────────────────────────────────────┐
│  🎓 ARCHITECTURE                                              │
└───────────────────────────────────────────────────────────────┘

   Browser (:3000)
        │
        ├─→ Next.js Frontend
        │   └─→ React Pages
        │       └─→ Zustand Store
        │           └─→ Fetch API
        │
        └─→ FastAPI Backend (:8080)
            └─→ Auth Routes (/auth/*)
            └─→ Waste Routes (/api/v1/waste/*)
            └─→ Database (PostgreSQL/SQLite)

┌───────────────────────────────────────────────────────────────┐
│  📚 DOCUMENTATION                                             │
└───────────────────────────────────────────────────────────────┘

START_HERE_LOCAL.md    → Quick 2-step guide
LOCAL_DEV_GUIDE.md     → Comprehensive developer guide
SETUP_COMPLETE.md      → Full setup documentation
DEPLOYMENT_STATUS.md   → Current status & checklist

┌───────────────────────────────────────────────────────────────┐
│  ✅ READY TO DEVELOP                                          │
└───────────────────────────────────────────────────────────────┘

Backend changes → Auto-reload enabled
Frontend changes → Hot Module Replacement (HMR)

Start coding:
  Backend:  backend/app/
  Frontend: frontend/src/

Happy coding! 🎉

═══════════════════════════════════════════════════════════════
